# API Contracts: Activity Graph Visualization

This directory contains interface definitions and contracts for the Activity Graph feature components and utilities.

## Directory Structure

- `components.md` — React component interfaces and prop types
- `utilities.md` — Pure function signatures for data transformation and color mapping

## Overview

All contracts follow TypeScript strict mode conventions and align with existing codebase patterns.
