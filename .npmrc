engine-strict=true
# tree-sitter-ruby@0.23.1 (latest) still declares a peer on tree-sitter@^0.21.1;
# it's compatible at runtime with tree-sitter@^0.25 but hasn't published an
# updated peer range. Without this, `npm ci` fails with ERESOLVE.
legacy-peer-deps=true
